# 🚀 台灣股市投資分析系統 - 部署指南

**版本**: v2.0 Professional
**更新日期**: 2026-01-14

---

## 📋 目錄

1. [部署方案比較](#部署方案比較)
2. [推薦方案：Streamlit Community Cloud](#推薦方案streamlit-community-cloud)
3. [方案二：Docker 容器化部署](#方案二docker-容器化部署)
4. [方案三：雲端平台部署](#方案三雲端平台部署)
5. [方案四：本地伺服器部署](#方案四本地伺服器部署)
6. [安全性考量](#安全性考量)
7. [效能優化](#效能優化)

---

## 📊 部署方案比較

| 方案 | 難度 | 成本 | 適合場景 | 推薦指數 |
|------|------|------|----------|----------|
| **Streamlit Cloud** | ⭐ | 免費 | 小型專案、展示 | ⭐⭐⭐⭐⭐ |
| **Docker + VPS** | ⭐⭐⭐ | $5-20/月 | 中型專案、需要客製化 | ⭐⭐⭐⭐ |
| **AWS/GCP/Azure** | ⭐⭐⭐⭐ | 按用量計費 | 企業級、高可用 | ⭐⭐⭐ |
| **本地伺服器** | ⭐⭐ | 硬體成本 | 內網使用、資料敏感 | ⭐⭐⭐ |

### 🏆 最佳推薦

**對於此系統，我強烈推薦使用 Streamlit Community Cloud**，原因：

1. ✅ **完全免費** - 公開專案免費託管
2. ✅ **零配置** - 直接從 GitHub 部署
3. ✅ **自動更新** - 推送代碼自動重新部署
4. ✅ **HTTPS** - 自動提供 SSL 憑證
5. ✅ **簡單分享** - 直接提供公開 URL

---

## 🌟 推薦方案：Streamlit Community Cloud

### 步驟 1：準備 GitHub 儲存庫

```bash
# 1. 初始化 Git（如果尚未初始化）
cd d:\stockIDE
git init

# 2. 建立 .gitignore
# （已經建立）

# 3. 提交代碼
git add .
git commit -m "Initial commit: Taiwan Stock Analyzer v2.0"

# 4. 建立 GitHub 儲存庫並推送
# 前往 https://github.com/new 建立新儲存庫
git remote add origin https://github.com/YOUR_USERNAME/taiwan-stock-analyzer.git
git branch -M main
git push -u origin main
```

### 步驟 2：部署到 Streamlit Cloud

1. **前往 Streamlit Cloud**
   - 網址：https://share.streamlit.io/

2. **登入 GitHub 帳號**
   - 點擊 "Sign in with GitHub"
   - 授權 Streamlit 存取您的儲存庫

3. **建立新應用**
   - 點擊 "New app"
   - 選擇您的儲存庫：`YOUR_USERNAME/taiwan-stock-analyzer`
   - 選擇分支：`main`
   - 主檔案路徑：`app.py`
   - 點擊 "Deploy!"

4. **等待部署完成**
   - 約 2-5 分鐘
   - 部署完成後會得到 URL：`https://YOUR_APP_NAME.streamlit.app`

### 步驟 3：分享給使用者

部署完成後，您會得到一個公開 URL，例如：
```
https://taiwan-stock-analyzer.streamlit.app
```

任何人都可以通過這個 URL 直接使用您的系統！

### Streamlit Cloud 進階設定

#### 設定 Secrets（如有 API Key）

1. 在 Streamlit Cloud 控制台點擊您的應用
2. 點擊 "Settings" → "Secrets"
3. 添加密鑰：
```toml
# .streamlit/secrets.toml 格式
[api_keys]
finmind_token = "your_token_here"
```

#### 設定資源限制

Streamlit Cloud 免費版限制：
- 記憶體：1GB
- 儲存空間：1GB
- 休眠機制：閒置後會休眠，首次訪問需等待喚醒

---

## 🐳 方案二：Docker 容器化部署

適合需要更多控制權或部署在自己的伺服器上。

### 本地測試

```bash
# 1. 建構映像
docker build -t taiwan-stock-analyzer .

# 2. 運行容器
docker run -p 8501:8501 taiwan-stock-analyzer

# 3. 開啟瀏覽器
# http://localhost:8501
```

### 使用 Docker Compose

```bash
# 啟動服務
docker-compose up -d

# 查看日誌
docker-compose logs -f

# 停止服務
docker-compose down
```

### 部署到 VPS（以 DigitalOcean 為例）

#### 1. 建立 Droplet

- 前往 https://www.digitalocean.com/
- 建立 Droplet（$6/月 方案即可）
- 選擇 Ubuntu 22.04
- 選擇區域（建議選擇亞洲區域如新加坡）

#### 2. 安裝 Docker

```bash
# SSH 連接到伺服器
ssh root@YOUR_SERVER_IP

# 安裝 Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 安裝 Docker Compose
sudo apt install docker-compose -y
```

#### 3. 部署應用

```bash
# 克隆專案
git clone https://github.com/YOUR_USERNAME/taiwan-stock-analyzer.git
cd taiwan-stock-analyzer

# 啟動服務
docker-compose up -d

# 設定防火牆
sudo ufw allow 8501
```

#### 4. 設定域名（可選）

使用 Nginx 反向代理 + Let's Encrypt SSL：

```bash
# 安裝 Nginx
sudo apt install nginx -y

# 建立配置
sudo nano /etc/nginx/sites-available/stock-analyzer
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
```

```bash
# 啟用配置
sudo ln -s /etc/nginx/sites-available/stock-analyzer /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 安裝 SSL（免費）
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

---

## ☁️ 方案三：雲端平台部署

### AWS (Amazon Web Services)

#### 使用 AWS App Runner（推薦）

1. 建立 ECR 儲存庫並推送映像
2. 建立 App Runner 服務
3. 自動擴展、負載平衡

**預估成本**：$15-30/月

#### 使用 EC2

1. 建立 EC2 實例（t3.small 即可）
2. 安裝 Docker
3. 部署容器

**預估成本**：$15-25/月

### Google Cloud Platform

#### 使用 Cloud Run（推薦）

```bash
# 安裝 gcloud CLI
# 建構並推送映像
gcloud builds submit --tag gcr.io/PROJECT_ID/stock-analyzer

# 部署
gcloud run deploy stock-analyzer \
  --image gcr.io/PROJECT_ID/stock-analyzer \
  --platform managed \
  --region asia-east1 \
  --allow-unauthenticated
```

**預估成本**：按用量計費，小流量可能免費

### Microsoft Azure

#### 使用 Azure Container Instances

```bash
# 建立資源群組
az group create --name stock-analyzer-rg --location eastasia

# 部署容器
az container create \
  --resource-group stock-analyzer-rg \
  --name stock-analyzer \
  --image YOUR_IMAGE \
  --ports 8501 \
  --dns-name-label stock-analyzer
```

**預估成本**：$10-20/月

---

## 🖥️ 方案四：本地伺服器部署

適合內網使用或對資料安全有特殊需求。

### Windows 伺服器

```powershell
# 1. 安裝 Python 3.11+
# 下載 https://www.python.org/downloads/

# 2. 建立虛擬環境
python -m venv venv
.\venv\Scripts\activate

# 3. 安裝依賴
pip install -r requirements.txt

# 4. 啟動服務
streamlit run app.py --server.port 8501 --server.address 0.0.0.0
```

### 設定為 Windows 服務

使用 NSSM（Non-Sucking Service Manager）：

```powershell
# 1. 下載 NSSM
# https://nssm.cc/download

# 2. 安裝服務
nssm install StockAnalyzer "C:\path\to\venv\Scripts\streamlit.exe" "run app.py --server.port 8501"

# 3. 設定工作目錄
nssm set StockAnalyzer AppDirectory "D:\stockIDE"

# 4. 啟動服務
nssm start StockAnalyzer
```

### Linux 伺服器（使用 systemd）

```bash
# 建立服務檔案
sudo nano /etc/systemd/system/stock-analyzer.service
```

```ini
[Unit]
Description=Taiwan Stock Analyzer
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/stock-analyzer
ExecStart=/opt/stock-analyzer/venv/bin/streamlit run app.py --server.port 8501 --server.address 0.0.0.0
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# 啟用並啟動服務
sudo systemctl daemon-reload
sudo systemctl enable stock-analyzer
sudo systemctl start stock-analyzer
```

---

## 🔒 安全性考量

### 1. 基本安全措施

```python
# 在 app.py 開頭加入
import streamlit as st

# 啟用 XSRF 保護（已在 config.toml 設定）
# enableXsrfProtection = true
```

### 2. 密碼保護（可選）

如需限制存取，可加入簡單的密碼保護：

```python
# 在 app.py 開頭加入
def check_password():
    """簡單的密碼保護"""
    def password_entered():
        if st.session_state["password"] == st.secrets.get("password", "your_password"):
            st.session_state["password_correct"] = True
            del st.session_state["password"]
        else:
            st.session_state["password_correct"] = False

    if "password_correct" not in st.session_state:
        st.text_input("請輸入密碼", type="password", on_change=password_entered, key="password")
        return False
    elif not st.session_state["password_correct"]:
        st.text_input("請輸入密碼", type="password", on_change=password_entered, key="password")
        st.error("密碼錯誤")
        return False
    else:
        return True

# 在 main() 函數開頭
if not check_password():
    st.stop()
```

### 3. API 限制

考慮加入使用限制以防止濫用：

```python
# 限制每分鐘請求次數
from datetime import datetime, timedelta

if 'last_request' not in st.session_state:
    st.session_state.last_request = datetime.now() - timedelta(seconds=60)

if (datetime.now() - st.session_state.last_request).seconds < 5:
    st.warning("請求過於頻繁，請稍後再試")
    st.stop()

st.session_state.last_request = datetime.now()
```

---

## ⚡ 效能優化

### 1. 快取設定

確保使用 Streamlit 的快取功能：

```python
@st.cache_data(ttl=3600)  # 快取 1 小時
def get_stock_data(stock_id, days):
    return data_fetcher.get_stock_price(stock_id, days)
```

### 2. 資源限制

在 docker-compose.yml 中設定資源限制：

```yaml
deploy:
  resources:
    limits:
      cpus: '2'
      memory: 2G
```

### 3. 監控

使用 Streamlit 內建的監控：

```bash
# 查看應用狀態
curl http://localhost:8501/_stcore/health
```

---

## 📝 快速部署檢查清單

### Streamlit Cloud 部署

- [ ] 建立 GitHub 儲存庫
- [ ] 確認 requirements.txt 正確
- [ ] 確認 app.py 在根目錄
- [ ] 登入 Streamlit Cloud
- [ ] 連結 GitHub 儲存庫
- [ ] 部署應用
- [ ] 測試 URL

### Docker 部署

- [ ] 安裝 Docker
- [ ] 建構映像：`docker build -t stock-analyzer .`
- [ ] 測試容器：`docker run -p 8501:8501 stock-analyzer`
- [ ] 推送到伺服器
- [ ] 設定防火牆
- [ ] 設定域名（可選）
- [ ] 設定 SSL（可選）

---

## 🆘 常見問題

### Q1: Streamlit Cloud 部署失敗？

**A**: 檢查：
1. requirements.txt 是否正確
2. Python 版本是否相容
3. 是否有缺少的依賴

### Q2: Docker 建構失敗？

**A**: 檢查：
1. Dockerfile 語法是否正確
2. 基礎映像是否可用
3. pip install 是否有錯誤

### Q3: 訪問速度很慢？

**A**: 考慮：
1. 選擇較近的伺服器區域
2. 增加快取時間
3. 優化資料查詢

### Q4: 如何更新部署？

**A**:
- Streamlit Cloud：推送到 GitHub 自動更新
- Docker：重新建構映像並重啟容器

---

## 📞 技術支援

如有部署問題，請檢查：

1. [Streamlit 官方文件](https://docs.streamlit.io/streamlit-community-cloud)
2. [Docker 官方文件](https://docs.docker.com/)
3. 本專案的 README.md

---

**祝您部署順利！** 🚀

**系統版本**: v2.0 Professional
**更新日期**: 2026-01-14
